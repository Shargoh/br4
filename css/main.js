import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  input: {
    height: 30,
    width: 200,
    backgroundColor: '#fff',
    textAlign: 'center',
  },
  listitem: {
    padding: 50,
    alignItems: 'center',
    justifyContent: 'center',
  }
});
